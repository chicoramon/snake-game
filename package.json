{
  "name": "snake-game",
  "private": true,
  "version": "1.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vite build && node scripts/package-static-assets.mjs && node scripts/build-service-worker.mjs",
    "preview": "vite preview",
    "test": "node --test tests/*.test.js tests/*.test.mjs",
    "test:smoke": "playwright test"
  },
  "devDependencies": {
    "@playwright/test": "^1.55.0",
    "vite": "^7.1.7"
  }
}
